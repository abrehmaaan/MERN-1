var url = "Http://google.com";
function log(message){
    console.log(message);
}

module.exports.log = log;
//first log is endpoint i.e the name in other file second log is name in this file

// module.exports.url = url;